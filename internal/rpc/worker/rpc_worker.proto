syntax = "proto3";
option go_package = "github.com/sapcc/andromeda/internal/rpc/worker";

service RPCWorker {
  rpc SyncAll(SyncRequest) returns (SyncResponse) {}
}

message SyncRequest {
}

message SyncResponse {
  bool accepted = 1;
}
